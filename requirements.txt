# Core pipeline dependencies for jcim.
#
# Pinned to versions verified to work together as of 2026-09; see README's
# "Environment Notes" section for background on why these pins matter.
numpy<2
pandas
scipy
scikit-learn>=1.3,<1.5
rdkit
torch==2.14.0
skorch==1.4.0
